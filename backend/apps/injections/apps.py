from django.apps import AppConfig


class InjectionsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.injections"
    label = "injections"
    verbose_name = "Injections"
